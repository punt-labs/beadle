# LaTeX build artifacts
*.aux
*.bbl
*.bcf
*.blg
*.log
*.out
*.run.xml

# TTS ephemeral audio
.tts/
.vox/

# Claude local settings
.claude/settings.local.json
.claude/*.local.md

# direnv
.envrc
!.claude/settings.json
!.claude/hooks/

# Scratch/temp directory (TMPDIR set via .envrc)
.tmp/

# Beadle runtime data (downloaded attachments, etc.)
.beadle/

# Go build artifacts
/beadle-email
dist/
coverage.out
.biff.local

# MCP (project-local, not part of plugin distribution)
.mcp.json
