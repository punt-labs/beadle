# LaTeX build artifacts
*.aux
*.bbl
*.bcf
*.blg
*.log
*.out
*.run.xml
*.pdf
*.toc
*.synctex.gz

# TTS ephemeral audio
.tts/

# Claude local settings
.claude/settings.local.json
.claude/*.local.md
.claude/beadle.poll.ts
.claude/projects/
.claude/worktrees/
.claude/scheduled_tasks.json
.claude/scheduled_tasks.lock

# direnv
!.claude/settings.json
!.claude/hooks/

# Scratch/temp directory (TMPDIR set via .envrc)
.tmp/

# Session transcripts
*-transcript.txt

# Go build artifacts
/beadle-email
dist/
coverage.out
.biff.local

# MCP (project-local, not part of plugin distribution)
.mcp.json
docs/fuzz.sty
docs/*.mf
docs/*.pk
docs/*.tfm
docs/*.aux
docs/*.log
docs/*.fuzz
docs/*.toc
.envrc.local

# Quarry session-transcript captures (pkit-kcps)
.punt-labs/quarry/captures/

# Ethos per-session and per-mission local state. Machine-local by
# construction — one file per session id, written live while a session runs.
# Tracking it races every git operation that needs a clean tree (a release
# preflight, a branch checkout) against the running session's own writer.
.punt-labs/local/

# The mandated sibling lock every punt CLI takes before mutating a shared
# host file (tool-enable-disable.md 2.4). Per-machine runtime state, and it
# persists by design — unlinking a lock file races the next writer. The
# pattern is broad because the lock's name is the host file's, and tools
# differ on the suffix — .CLAUDE.md.lock and .CLAUDE.md.punt-import.lock
# both appear.
.*.lock
